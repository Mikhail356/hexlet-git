'''
async def main():
    server = web.Server(handler)
    runner = web.ServerRunner(server)
    task1 = runner.setup()
    site = web.TCPSite(runner, 'localhost', 8080)
    task2 = site.start()

    print("======= Serving on http://127.0.0.1:8080/ ======")

    # pause here for very long time by serving HTTP requests and
    # waiting for keyboard interruption
    task3 = asyncio.sleep(100*3600)
    await asyncio.all([task1, task2, task3])
'''

Вытесняющая — ОС (процессы и потоки) = каждый квант планирования
(в Linux раньше 100Hz) ОС прерывает текущий процесс и отдаёт управление другому.

Кооперативная — каждый процесс сам решает, когда отдать управление другим
(async/await; Windows 3.11)
Сейчас начинает использоваться вместо синхронных однопоточных программ.

io-uring (I/O user-mode ring buffers) в ядре Linux 5+
https://lwn.net/Articles/810414/
https://kernel.dk/io_uring.pdf

Э. Таненбаум. Операционные системы. Разработка и реализация


Параллельно теория и реализация

I Теория: описать протокол взаимодействия между узлами распределённой системы
1. Описать что происходит в стандартных сценариях Create, Update и Delete (создание новой записи, изменение записи и удаление записи)
Ограничения на первое время: узлы «обычные», которые хранят данные.
Пример — информация о научной статье (заголовок, авторы, журнал)
Несколько авторов работают в нескольких организациях (в разных сочетаниях).

Первый вариант: markdown в репозитории

На будущее:
https://en.wikipedia.org/wiki/Business_Process_Model_and_Notation

Create (SERVER1) (ссылается на SERVER2, SERVER3)
    1. создаётся html-файл на SERVER1
    2. отправляете (curl) запрос WebMention, который говорит, что новый html-файл ссылается на SERVER2  
    3. --- SERVER3

    4. SERVER2 проверяет, какие на него свежие упоминания — тем самым узнаёт о новом html-файле на SERVER1


II Реализация: макет, демонстрирующий теорию.
Несколько экземпляров «системы» с данными в microformats (можно статические — nginx или darkhttpd) — либо в разных подкаталогах одного сервера, либо на разных портах.
WebMention и WebSub-узлы (готовые сервера)  -> (вручную?)
- последовательность ручных действий для моделирования
- скрипты для фрагментов алгоритма

* каталоги с данными — в репозитории; скрипт для запуска нужного количества экземпляров darkhttpd на правильных портах

III Текст: 
- введение
- обзорная часть про стандарты и технологии
- теория
- реализация
- заключение

pandoc : markdown → LaTeX

DNS: символьное имя github.com → ip-адрес
Порт: число 1-65535, обозначающее конкретную программу-сервер, которая на этом IP-адресе запущена

